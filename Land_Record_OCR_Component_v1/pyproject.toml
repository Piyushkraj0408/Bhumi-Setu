[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "land-record-ocr"
version = "0.1.0"
description = "Modular Hindi/English land-record OCR component"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
