[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "land-record-mistral-ocr"
version = "1.0.0"
description = "Hindi/English land-record OCR using Mistral OCR API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
