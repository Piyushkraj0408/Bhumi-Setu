# Database Configuration
# MongoDB Atlas / Cluster Connection
MONGODB_URI=mongodb+srv://<piyushobroy87_db_user>:<BKrgsM9VgRsAdbYd>@cluster0.xxxxx.mongodb.net/land_records?retryWrites=true&w=majority
MONGODB_DB_NAME=land_records

# (Optional) SQL Fallback
DATABASE_URL=sqlite:///./land_records.db

# Auth & JWT Configuration
SECRET_KEY=dev-secret-key-change-in-production-091823746182
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Object storage (MinIO locally / S3 in prod / Local Storage fallback)
S3_ENDPOINT_URL=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET_NAME=land-records-documents
S3_REGION=us-east-1
USE_LOCAL_STORAGE_FALLBACK=True
LOCAL_STORAGE_DIR=./storage
